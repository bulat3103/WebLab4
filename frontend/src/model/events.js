import {createEvent} from "effector";

export const updateUsername = createEvent();
export const updatePassword = createEvent();

export const updateIsLogged = createEvent();

export const updateWarnMessage = createEvent();
export const updateWarnMainPageMessage = createEvent();

export const updatePoints = createEvent();
export const resetPoints = createEvent();

export const updateX = createEvent();
export const updateY = createEvent();
export const updateR = createEvent();